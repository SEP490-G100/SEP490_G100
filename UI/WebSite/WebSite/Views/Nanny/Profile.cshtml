@model WebSite.Models.Nanny.NannyProfileViewModel
@{
    ViewData["Title"] = "Ho so bao mau";
}

<div class="min-h-screen bg-gradient-to-br from-orange-50 via-amber-50/30 to-white py-10 px-4">
    <div class="max-w-3xl mx-auto">
        <div class="mb-6 text-center">
            <span class="section-label">Ho so bao mau</span>
            <h1 class="text-3xl font-bold text-gray-900 mt-3" style="font-family:'Quicksand',sans-serif">
                Buoc 2: Kinh nghiem va ky vong cong viec
            </h1>
            <div class="section-divider mt-4 mx-auto"></div>
        </div>

        <div class="bg-white/90 rounded-3xl border border-orange-100 shadow-soft overflow-hidden">
            <div class="h-1 bg-orange-100">
                <div class="h-1 bg-gradient-to-r from-orange-500 to-amber-500" style="width:33%"></div>
            </div>

            <div class="p-8">
                <div asp-validation-summary="ModelOnly" class="text-red-500 text-sm mb-4"></div>

                <form id="nannyProfileForm" asp-action="Profile" asp-controller="Nanny" method="post">
                    @Html.AntiForgeryToken()

                    <div class="space-y-5">
                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Gioi thieu ban than</label>
                            <textarea asp-for="Bio" rows="4"
                                      class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400"
                                      placeholder="Chia se ngan gon ve ban..."></textarea>
                            <span asp-validation-for="Bio" class="text-red-500 text-xs mt-1"></span>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">So nam kinh nghiem</label>
                                <input asp-for="YearsOfExperience" type="number" min="0"
                                       class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400" />
                                <span asp-validation-for="YearsOfExperience" class="text-red-500 text-xs mt-1"></span>
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Trinh do hoc van</label>
                                <select asp-for="EducationLevel"
                                        class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400">
                                    <option value="">-- Chon trinh do hoc van --</option>
                                    <option value="0">Trung hoc</option>
                                    <option value="1">Cao dang</option>
                                    <option value="2">Dai hoc</option>
                                    <option value="3">Thac si</option>
                                    <option value="4">Khac</option>
                                </select>
                                <span asp-validation-for="EducationLevel" class="text-red-500 text-xs mt-1"></span>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Muc luong toi thieu</label>
                                <div class="relative">
                                    <input type="hidden" asp-for="ExpectedSalaryMin" />
                                    <input id="ExpectedSalaryMinDisplay" type="text" inputmode="numeric" autocomplete="off" value="@(Model.ExpectedSalaryMin?.ToString("0") ?? "")"
                                           class="w-full px-4 py-3 pr-16 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400" />
                                    <span class="absolute right-4 top-1/2 -translate-y-1/2 text-xs font-bold text-gray-500">VND</span>
                                </div>
                                <span asp-validation-for="ExpectedSalaryMin" class="text-red-500 text-xs mt-1"></span>
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">Muc luong toi da</label>
                                <div class="relative">
                                    <input type="hidden" asp-for="ExpectedSalaryMax" />
                                    <input id="ExpectedSalaryMaxDisplay" type="text" inputmode="numeric" autocomplete="off" value="@(Model.ExpectedSalaryMax?.ToString("0") ?? "")"
                                           class="w-full px-4 py-3 pr-16 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400" />
                                    <span class="absolute right-4 top-1/2 -translate-y-1/2 text-xs font-bold text-gray-500">VND</span>
                                </div>
                                <span asp-validation-for="ExpectedSalaryMax" class="text-red-500 text-xs mt-1"></span>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500">Luong hop le tu 8.000.000 den 50.000.000 VND.</p>
                        <div id="salaryRangeError" class="hidden text-red-500 text-xs bg-red-50 p-3 rounded-lg border border-red-200"></div>

                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Khoang cach di chuyen toi da (km)</label>
                            <input asp-for="MaxTravelDistance" type="number"
                                   class="w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 text-gray-900 focus:outline-none focus:ring-2 focus:ring-orange-400" />
                            <span asp-validation-for="MaxTravelDistance" class="text-red-500 text-xs mt-1"></span>
                        </div>
                    </div>

                    <div class="flex justify-between items-center mt-8">
                        <a href="@Url.Action("Step1BasicInfo", "NannyBasicInfo")"
                           class="px-5 py-2 rounded-full border border-orange-200 text-gray-700 hover:bg-orange-50 text-sm font-semibold">
                            Quay lai
                        </a>
                        <button type="submit"
                                class="px-6 py-2 rounded-full bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white font-semibold text-sm shadow-md">
                            Tiep tuc
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
(function () {
    var form = document.getElementById('nannyProfileForm');
    var minEl = document.getElementById('ExpectedSalaryMinDisplay');
    var maxEl = document.getElementById('ExpectedSalaryMaxDisplay');
    var minHiddenEl = document.getElementById('ExpectedSalaryMin');
    var maxHiddenEl = document.getElementById('ExpectedSalaryMax');
    var errEl = document.getElementById('salaryRangeError');
    var minSalaryAllowed = 8000000;
    var maxSalaryAllowed = 50000000;

    function onlyDigits(value) {
        return String(value || '').replace(/\D/g, '');
    }

    function formatCurrencyInput(value) {
        var digits = onlyDigits(value);
        if (!digits) return '';
        return digits.replace(/\B(?=(\d{3})+(?!\d))/g, '.');
    }

    function parseCurrencyValue(value) {
        var digits = onlyDigits(value);
        return digits ? Number(digits) : null;
    }

    function bindCurrencyInput(input) {
        if (!input) return;

        input.value = formatCurrencyInput(input.value);

        input.addEventListener('input', function () {
            input.value = formatCurrencyInput(input.value);
            syncHiddenValues();
            showErr(validate());
        });

            input.value = formatCurrencyInput(input.value);
            syncHiddenValues();
            showErr(validate());
        });
    }

    function showErr(msg) {
        if (!errEl) return;
        errEl.textContent = msg || '';
        errEl.classList.toggle('hidden', !msg);
    }

    function validate() {
        var min = minEl ? parseCurrencyValue(minEl.value) : null;
        var max = maxEl ? parseCurrencyValue(maxEl.value) : null;

        if (min != null && (min < minSalaryAllowed || min > maxSalaryAllowed)) return 'Muc luong toi thieu phai trong khoang 8.000.000 - 50.000.000 VND.';
        if (max != null && (max < minSalaryAllowed || max > maxSalaryAllowed)) return 'Muc luong toi da phai trong khoang 8.000.000 - 50.000.000 VND.';
        if (min != null && max != null && max < min) return 'Muc luong toi da phai lon hon hoac bang muc toi thieu.';
        return '';
    }

    bindCurrencyInput(minEl);
    bindCurrencyInput(maxEl);

    function syncHiddenValues() {
        if (minHiddenEl) minHiddenEl.value = onlyDigits(minEl ? minEl.value : '');
        if (maxHiddenEl) maxHiddenEl.value = onlyDigits(maxEl ? maxEl.value : '');
    }

    syncHiddenValues();
    showErr(validate());

    if (form) {
        form.addEventListener('submit', function (e) {
            var msg = validate();
            showErr(msg);
            if (msg) {
                e.preventDefault();
                return;
            }

            syncHiddenValues();
        });
    }
})();
</script>

<partial name="~/Views/Hiring/_HireModal.cshtml" />