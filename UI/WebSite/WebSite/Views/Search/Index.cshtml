@{
    ViewData["Title"] = "Tìm bảo mẫu - NannyMatch";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isAuthenticated = User.Identity?.IsAuthenticated ?? false;
    var isParent = User.IsInRole("Parent");
    var isNanny = User.IsInRole("Nanny");
    var userName = User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? "";
    var skillOptions = ViewBag.SkillOptions as IEnumerable<WebSite.Models.Search.JobSkillOption>
        ?? System.Linq.Enumerable.Empty<WebSite.Models.Search.JobSkillOption>();
}

@section Head {
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <link rel="stylesheet" href="~/css/search.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/recommendation.css" asp-append-version="true" />
}

<div class="sticky top-[72px] z-40 bg-white/95 backdrop-blur-lg border-b border-gray-100" style="box-shadow:0 2px 8px rgba(0,0,0,.04);">
    <div class="px-6 py-3 flex items-center gap-3 flex-wrap">
        @* â”€â”€ Tab bar (hiá»‡n vá»›i Nanny) â€” ngoÃ i cÃ¹ng bÃªn trÃ¡i â”€â”€ *@
        @if (User.IsInRole("Nanny"))
        {
            <div class="rec-tab-bar">
                <button id="tabAll" class="rec-tab active" onclick="switchSearchTab('all')">
                    <span class="material-icons-round">list</span>Táº¥t cáº£
                </button>
                <button id="tabRec" class="rec-tab" onclick="switchSearchTab('rec')">
                    <span class="material-icons-round">auto_awesome</span>Gợi ý
                </button>
            </div>
        }
        <div class="relative flex-1 min-w-[240px]">
            <span class="material-icons-round absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-[18px]">search</span>
            <input id="searchCity" type="text" placeholder="TÃ¬m theo thÃ nh phá»‘..."
                   class="w-full pl-9 pr-4 py-2 rounded-xl border border-gray-200 bg-gray-50 text-sm font-semibold text-gray-800 focus:ring-2 focus:ring-orange-300 focus:border-orange-400 outline-none transition-all placeholder-gray-400"
                   oninput="debounceSearch()" />
        </div>

        <span id="resultCount" class="text-sm text-gray-400 font-semibold whitespace-nowrap">Äang táº£i...</span>
        @if (isParent)
        {
            <button onclick="openHistory()"
                    class="flex items-center gap-1.5 px-4 py-2 rounded-xl border border-orange-200 bg-white hover:bg-orange-50 text-orange-600 text-sm font-bold transition-all">
                <span class="material-icons-round text-base">history</span>Lá»‹ch sá»­ bÃ i Ä‘Äƒng
            </button>
            <button onclick="openCreate()"
                    class="flex items-center gap-1.5 px-4 py-2 rounded-xl bg-orange-500 hover:bg-orange-600 text-white text-sm font-bold shadow-md hover:shadow-lg transition-all">
                <span class="material-icons-round text-base">add_circle</span>ÄÄƒng tin
            </button>
        }
        @if (isNanny)
        {
            <a href="/Search/Applications"
               class="flex items-center gap-1.5 px-4 py-2 rounded-xl border border-slate-200 bg-white hover:bg-slate-50 text-slate-700 text-sm font-bold transition-all">
                <span class="material-icons-round text-base">work_history</span>Lá»‹ch sá»­ á»©ng tuyá»ƒn
            </a>
            <a href="/Search/SavedJobs"
               class="flex items-center gap-1.5 px-4 py-2 rounded-xl border border-orange-200 bg-white hover:bg-orange-50 text-orange-600 text-sm font-bold transition-all">
                <span class="material-icons-round text-base">bookmark</span>BÃ i Ä‘Äƒng Ä‘Ã£ lÆ°u
            </a>
        }
        <button onclick="openPremium()"
                class="flex items-center gap-1.5 px-3 py-2 rounded-xl text-white text-sm font-bold shadow-md transition-all"
                style="background:linear-gradient(135deg,#f59e0b,#d97706)">
            <span class="material-icons-round text-[16px]">workspace_premium</span>Premium
        </button>
    </div>
</div>

<div class="search-body">
    <div class="list-panel">

        @* â”€â”€ Rec panel (Tab 2) â€” chá»‰ render vá»›i Nanny â”€â”€ *@
        @if (User.IsInRole("Nanny"))
        {
            <div id="recJobPanel" class="rec-panel">
                <div id="recJobContent">
                    <div class="rec-panel__loading">
                        <span class="material-icons-round" style="font-size:22px;opacity:.4">hourglass_top</span>
                        Đang tải gợi ý...
                    </div>
                </div>
            </div>
        }

        <div id="jobList"></div>
    </div>
    <div class="map-panel">
        <div id="map"></div>
    </div>
</div>

<div id="createModal" class="nmodal fixed inset-0 z-50 bg-black/50 backdrop-blur-sm items-center justify-center px-4" onclick="if(event.target===this)closeCreate()">
    <div class="nmodal-box modal-shell bg-white rounded-2xl w-full max-w-5xl shadow-2xl relative">
        <div class="modal-head">
            <div>
                <p class="modal-kicker">BÃ i Ä‘Äƒng má»›i</p>
                <h2 class="text-2xl font-bold text-gray-900">ÄÄƒng tin tÃ¬m báº£o máº«u</h2>
                <p class="modal-subtitle">Táº¡o bÃ i Ä‘Äƒng rÃµ rÃ ng Ä‘á»ƒ moderator duyá»‡t nhanh hÆ¡n vÃ  nanny dá»… á»©ng tuyá»ƒn hÆ¡n.</p>
            </div>
            <button onclick="closeCreate()" class="modal-close material-icons-round">close</button>
        </div>
        <div class="modal-scroll">
            <form id="createForm" class="modal-form">
                <div class="section-card">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Thông tin cơ bản</p>
                            <p class="section-caption">Ná»™i dung bÃ i Ä‘Äƒng, má»©c lÆ°Æ¡ng, Ä‘á»‹a Ä‘iá»ƒm vÃ  yÃªu cáº§u chÃ­nh.</p>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="md:col-span-2">
                            <label class="form-label">TiÃªu Ä‘á» bÃ i Ä‘Äƒng</label>
                            <input type="text" id="cf-title" class="form-input" required style="border-radius: 10px;" />
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Mô tả chi tiết</label>
                            <textarea id="cf-desc" class="form-input resize-none" rows="3" required></textarea>
                        </div>
                        <div>
                            <label class="form-label">Loáº¡i cÃ´ng viá»‡c</label>
                            <input type="text" id="cf-typeText" class="form-input form-select-picker-input" autocomplete="off" placeholder="Chá»n loáº¡i cÃ´ng viá»‡c" />
                            <select id="cf-type" class="form-input form-select-native-hidden">
                                <option value="1">Full-time</option>
                                <option value="2">Part-time</option>
                                <option value="3">Qua Ä‘Ãªm</option>
                            </select>
                        </div>
                        <div>
                            <label class="form-label">Sá»‘ tráº» cáº§n chÄƒm</label>
                            <select id="cf-children" class="form-input">
                                @for (var childCount = 1; childCount <= 10; childCount++)
                                {
                                    <option value="@childCount">@childCount bé</option>
                                }
                            </select>
                        </div>
                        <div>
                            <label class="form-label" >LÆ°Æ¡ng tá»‘i thiá»ƒu</label>
                            <input type="text" id="cf-salMin" inputmode="numeric" style="border-radius: 10px;" class="form-input" placeholder="VD: 1.000.000" />
                        </div>
                        <div>
                            <label class="form-label" >LÆ°Æ¡ng tá»‘i Ä‘a</label>
                            <input type="text" id="cf-salMax" inputmode="numeric" style="border-radius: 10px;" class="form-input" placeholder="VD: 3.000.000" />
                        </div>
                        <div class="md:col-span-2 text-xs font-semibold text-slate-500">
                            Äá»ƒ trá»‘ng náº¿u chÆ°a xÃ¡c Ä‘á»‹nh. LÆ°Æ¡ng há»£p lá»‡ tá»« 8.000.000 Ä‘áº¿n 50.000.000 VND.
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Tùy chọn lương</label>
                            <label class="negotiable-field negotiable-field-large">
                                <input type="checkbox" id="cf-negotiable" class="rounded accent-orange-500" />
                                <span class="negotiable-title">Lương thương lượng</span>
                                <span class="negotiable-caption">Báº­t náº¿u báº¡n muá»‘n trao Ä‘á»•i má»©c lÆ°Æ¡ng trá»±c tiáº¿p vá»›i nanny.</span>
                            </label>
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label" >Äá»‹a chá»‰ chi tiáº¿t</label>
                            <input type="text" id="cf-location" style="border-radius: 10px;" class="form-input" />
                        </div>
                        <div>
                            <label class="form-label">ThÃ nh phá»‘</label>
                            <input type="text" id="cf-city" style="border-radius: 10px;" class="form-input form-select-picker-input" autocomplete="off" placeholder="Nháº­p hoáº·c chá»n thÃ nh phá»‘" required />
                        </div>
                        <div>
                            <label class="form-label">Phường/Xã</label>
                            <input type="text" id="cf-district" style="border-radius: 10px;" class="form-input form-select-picker-input" autocomplete="off" placeholder="Nhập hoặc chọn phường/xã" />
                        </div>
                        <div>
                            <label class="form-label" >Äá»™ tuá»•i báº£o máº«u tá»«</label>
                            <input type="number" id="cf-minAge" class="form-input" style="border-radius: 10px;" min="18" max="80" />
                        </div>
                        <div>
                            <label class="form-label" >Äá»™ tuá»•i báº£o máº«u Ä‘áº¿n</label>
                            <input type="number" id="cf-maxAge" class="form-input" style="border-radius: 10px;" min="18" max="80" />
                        </div>
                        <div>
                            <label class="form-label">Tráº¡ng thÃ¡i bÃ i Ä‘Äƒng</label>
                            <div class="status-toggle" id="cf-statusToggle">
                                <button type="button" class="status-option active" onclick="setCreateStatus(1)">Công khai</button>
                                <button type="button" class="status-option" onclick="setCreateStatus(2)">áº¨n bÃ i Ä‘Äƒng</button>
                            </div>
                            <input type="hidden" id="cf-status" value="1" />
                        </div>
                    </div>
                </div>

                <div class="section-card schedule-card">
                    <div class="schedule-header">
                        <div>
                            <p class="section-title">Lá»‹ch ngÃ y muá»‘n tuyá»ƒn</p>
                            <p class="section-caption">Chá»n cÃ¡c khung giá» báº¡n muá»‘n nanny cÃ³ thá»ƒ lÃ m viá»‡c.</p>
                        </div>
                    </div>
                    <div id="cf-schedule" class="schedule-grid"></div>
                </div>

                <div class="section-card section-card-soft">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Thông tin từ profile Parent</p>
                            <p class="section-caption">Chá»n bÃ© thá»© 1 á»Ÿ Ä‘Ã¢y. Khi tÄƒng sá»‘ tráº», báº¡n cÃ³ thá»ƒ chá»n tá»«ng bÃ© vÃ  xem chi tiáº¿t táº¥t cáº£ cÃ¡c bÃ© bÃªn dÆ°á»›i.</p>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                        <div class="md:col-span-2">
                            <label class="form-label">Chọn bé thứ 1</label>
                            <input type="text" id="cf-childProfileText" class="form-input form-select-picker-input" autocomplete="off" placeholder="Chọn trẻ" />
                            <select id="cf-childProfileId" class="form-input form-select-native-hidden"></select>
                        </div>
                        <div>
                            <label class="form-label">Äáº·c Ä‘iá»ƒm</label>
                            <textarea id="cf-characteristic" class="form-input resize-none bg-white/80" rows="2" readonly></textarea>
                        </div>
                        <div>
                            <label class="form-label">NhÃ³m tuá»•i cá»§a bÃ©</label>
                            <input type="text" id="cf-birthType" class="form-input bg-white/80" readonly />
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Nhu cáº§u Ä‘áº·c biá»‡t</label>
                            <textarea id="cf-specialNeeds" class="form-input resize-none bg-white/80" rows="2" readonly></textarea>
                        </div>
                    </div>
                    <div id="cf-extraChildProfiles" class="child-profile-stack"></div>
                </div>

                <div class="section-card section-card-plain">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Ká»¹ nÄƒng yÃªu cáº§u</p>
                            <p class="section-caption">Chá»n tá»« danh sÃ¡ch ká»¹ nÄƒng cÃ³ sáºµn trong há»‡ thá»‘ng Ä‘á»ƒ bÃ i Ä‘Äƒng Ä‘á»“ng bá»™ hÆ¡n.</p>
                        </div>
                    </div>
                    <div class="flex gap-2 items-center">
                        <input type="text" id="cf-skillSelectText" class="form-input form-select-picker-input" autocomplete="off" placeholder="Chá»n ká»¹ nÄƒng cáº§n yÃªu cáº§u" />
                        <select id="cf-skillSelect" class="form-input form-select-native-hidden">
                            <option value="">Chá»n ká»¹ nÄƒng cáº§n yÃªu cáº§u</option>
                            @foreach (var skill in skillOptions)
                            {
                                <option value="@skill.SkillName">@skill.SkillName@(string.IsNullOrWhiteSpace(skill.Category) ? "" : $" - {skill.Category}")</option>
                            }
                        </select>
                        <button type="button" onclick="addCreateSkill()" class="icon-pill-btn">
                            <span class="material-icons-round text-base">add</span>
                        </button>
                    </div>
                    <div id="cf-skills" class="flex flex-wrap gap-2 mt-2"></div>
                </div>

            </form>
            <div class="modal-footer">
                <button type="button" onclick="closeCreate()" class="modal-btn modal-btn-secondary">Hủy</button>
                <button type="button" onclick="submitCreate()" class="modal-btn modal-btn-primary">ÄÄƒng bÃ i</button>
            </div>
        </div>
    </div>
</div>

<div id="editModal" class="nmodal fixed inset-0 z-50 bg-black/50 backdrop-blur-sm items-center justify-center px-4" onclick="if(event.target===this)closeEdit()">
    <div class="nmodal-box modal-shell bg-white rounded-2xl w-full max-w-5xl shadow-2xl relative">
        <div class="modal-head">
            <div>
                <p class="modal-kicker">Cáº­p nháº­t bÃ i Ä‘Äƒng</p>
                <h2 class="text-2xl font-bold text-gray-900">Chá»‰nh sá»­a bÃ i Ä‘Äƒng</h2>
                <p class="modal-subtitle">Sau khi cáº­p nháº­t, bÃ i Ä‘Äƒng sáº½ quay vá» tráº¡ng thÃ¡i chá» moderator duyá»‡t láº¡i.</p>
            </div>
            <button onclick="closeEdit()" class="modal-close material-icons-round">close</button>
        </div>
        <div class="modal-scroll">
            <form id="editForm" class="modal-form">
                <input type="hidden" id="ef-id" />
                <div class="section-card">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Thông tin cơ bản</p>
                            <p class="section-caption">Chá»‰nh sá»­a ná»™i dung, lÆ°Æ¡ng, Ä‘á»‹a Ä‘iá»ƒm vÃ  Ä‘á»™ tuá»•i nanny mong muá»‘n.</p>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="md:col-span-2">
                            <label class="form-label">TiÃªu Ä‘á» bÃ i Ä‘Äƒng</label>
                            <input type="text" id="ef-title" class="form-input" required />
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Mô tả chi tiết</label>
                            <textarea id="ef-desc" class="form-input resize-none" rows="3" required></textarea>
                        </div>
                        <div>
                            <label class="form-label">Loáº¡i cÃ´ng viá»‡c</label>
                            <select id="ef-type" class="form-input">
                                <option value="1">Full-time</option>
                                <option value="2">Part-time</option>
                                <option value="3">Qua Ä‘Ãªm</option>
                            </select>
                        </div>
                        <div>
                            <label class="form-label">Sá»‘ tráº» cáº§n chÄƒm</label>
                            <input type="number" id="ef-children" class="form-input bg-gray-50" min="1" max="10" readonly />
                        </div>
                        <div>
                            <label class="form-label">LÆ°Æ¡ng tá»‘i thiá»ƒu</label>
                            <input type="text" id="ef-salMin" inputmode="numeric" class="form-input" placeholder="VD: 1.000.000" />
                        </div>
                        <div>
                            <label class="form-label">LÆ°Æ¡ng tá»‘i Ä‘a</label>
                            <input type="text" id="ef-salMax" inputmode="numeric" class="form-input" placeholder="VD: 3.000.000" />
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Tùy chọn lương</label>
                            <label class="negotiable-field negotiable-field-large">
                                <input type="checkbox" id="ef-negotiable" class="rounded accent-orange-500" />
                                <span class="negotiable-title">Lương thương lượng</span>
                                <span class="negotiable-caption">Báº­t náº¿u báº¡n muá»‘n trao Ä‘á»•i má»©c lÆ°Æ¡ng trá»±c tiáº¿p vá»›i nanny.</span>
                            </label>
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Äá»‹a chá»‰ chi tiáº¿t</label>
                            <input type="text" id="ef-location" class="form-input" />
                        </div>
                        <div>
                            <label class="form-label">ThÃ nh phá»‘</label>
                            <input type="text" id="ef-city" class="form-input form-select-picker-input" autocomplete="off" placeholder="Nháº­p hoáº·c chá»n thÃ nh phá»‘" />
                        </div>
                        <div>
                            <label class="form-label">Phường/Xã</label>
                            <input type="text" id="ef-district" class="form-input form-select-picker-input" autocomplete="off" placeholder="Nhập hoặc chọn phường/xã" />
                        </div>
                        <div>
                            <label class="form-label">Äá»™ tuá»•i báº£o máº«u tá»«</label>
                            <input type="number" id="ef-minAge" class="form-input" min="18" max="80" />
                        </div>
                        <div>
                            <label class="form-label">Äá»™ tuá»•i báº£o máº«u Ä‘áº¿n</label>
                            <input type="number" id="ef-maxAge" class="form-input" min="18" max="80" />
                        </div>
                        <div>
                            <label class="form-label">Tráº¡ng thÃ¡i bÃ i Ä‘Äƒng</label>
                            <div class="status-toggle" id="ef-statusToggle">
                                <button type="button" class="status-option active" onclick="setEditStatus(1)">Công khai</button>
                                <button type="button" class="status-option" onclick="setEditStatus(2)">áº¨n bÃ i Ä‘Äƒng</button>
                            </div>
                            <input type="hidden" id="ef-status" value="1" />
                        </div>
                    </div>
                </div>

                <div class="section-card schedule-card">
                    <div class="schedule-header">
                        <div>
                            <p class="section-title">Lá»‹ch ngÃ y muá»‘n tuyá»ƒn</p>
                            <p class="section-caption">Cáº­p nháº­t cÃ¡c buá»•i cáº§n nanny há»— trá»£ trong tuáº§n.</p>
                        </div>
                    </div>
                    <div id="ef-schedule" class="schedule-grid"></div>
                </div>

                <div class="section-card section-card-soft">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Thông tin từ profile Parent</p>
                            <p class="section-caption">Chá»n láº¡i bÃ© náº¿u cáº§n, há»‡ thá»‘ng sáº½ cáº­p nháº­t thÃ´ng tin Hồ sơ trẻ tÆ°Æ¡ng á»©ng.</p>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                        <div class="md:col-span-2">
                            <label class="form-label">Chọn trẻ</label>
                            <select id="ef-childProfileId" class="form-input" onchange="handleEditChildChange()"></select>
                        </div>
                        <div>
                            <label class="form-label">Äáº·c Ä‘iá»ƒm</label>
                            <textarea id="ef-characteristic" class="form-input resize-none bg-white/80" rows="2" readonly></textarea>
                        </div>
                        <div>
                            <label class="form-label">NhÃ³m tuá»•i cá»§a bÃ©</label>
                            <input type="text" id="ef-birthType" class="form-input bg-white/80" readonly />
                        </div>
                        <div class="md:col-span-2">
                            <label class="form-label">Nhu cáº§u Ä‘áº·c biá»‡t</label>
                            <textarea id="ef-specialNeeds" class="form-input resize-none bg-white/80" rows="2" readonly></textarea>
                        </div>
                    </div>
                </div>

                <div class="section-card section-card-plain">
                    <div class="section-heading">
                        <div>
                            <p class="section-title">Ká»¹ nÄƒng yÃªu cáº§u</p>
                            <p class="section-caption">Tinh chá»‰nh bá»™ ká»¹ nÄƒng yÃªu cáº§u báº±ng danh sÃ¡ch skill chuáº©n cá»§a há»‡ thá»‘ng.</p>
                        </div>
                    </div>
                    <div class="flex gap-2 items-center">
                        <select id="ef-skillSelect" class="form-input">
                            @foreach (var skill in skillOptions)
                            {
                                <option value="@skill.SkillName">@skill.SkillName@(string.IsNullOrWhiteSpace(skill.Category) ? "" : $" - {skill.Category}")</option>
                            }
                            <option value="">Chá»n ká»¹ nÄƒng cáº§n yÃªu cáº§u</option>
                        </select>
                        <button type="button" onclick="addEditSkill()" class="icon-pill-btn">
                            <span class="material-icons-round text-base">add</span>
                        </button>
                    </div>
                    <div id="ef-skills" class="flex flex-wrap gap-2 mt-2"></div>
                </div>

            </form>
            <div class="modal-footer">
                <button type="button" onclick="deleteJob()" class="modal-btn modal-btn-danger flex items-center justify-center gap-1">
                    <span class="material-icons-round text-base">delete</span>Xóa
                </button>
                <button type="button" onclick="closeEdit()" class="modal-btn modal-btn-secondary">Hủy</button>
                <button type="button" onclick="submitEdit()" class="modal-btn modal-btn-primary">LÆ°u thay Ä‘á»•i</button>
            </div>
        </div>
    </div>
</div>

<div id="previewModal" class="nmodal fixed inset-0 z-50 bg-black/50 backdrop-blur-sm items-center justify-center px-4" onclick="if(event.target===this)closePreview()">
    <div class="nmodal-box search-detail-box bg-white rounded-[32px] w-full max-w-4xl shadow-2xl relative">
        <div class="modal-scroll">
            <div class="search-detail-close-wrap">
                <button onclick="closePreview()" class="search-detail-close material-icons-round">close</button>
            </div>
            <div class="search-detail-header">
                <div class="search-detail-hero">
                    <div class="search-detail-hero__content">
                        <div class="search-detail-badge">
                            <span class="material-icons-round text-[16px]">article</span>
                            Chi tiáº¿t tin Ä‘Äƒng
                        </div>
                        <h2 id="pv-title" class="search-detail-title">Ten bai viet</h2>
                        <p id="pv-parentName" class="search-detail-subtitle">Dang boi phu huynh</p>
                        <div id="pv-premiumBadges" class="mt-3 flex flex-wrap gap-2"></div>
                        <div class="search-detail-chips">
                            <span id="pv-type" class="search-detail-chip">Loai viec</span>
                            <span id="pv-sal" class="search-detail-chip search-detail-chip--strong">Luong</span>
                            <span id="pv-status" class="search-detail-chip search-detail-chip--muted">Trạng thái</span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="search-detail-grid">
                <section class="search-detail-card">
                    <h3>MÃ´ táº£ cÃ´ng viá»‡c</h3>
                    <p id="pv-desc" class="search-detail-text"></p>
                </section>
                <section class="search-detail-card">
                    <h3>Thông tin chính</h3>
                    <div class="search-detail-meta-list">
                        <div><span>Äá»‹a Ä‘iá»ƒm</span><strong id="pv-loc"></strong></div>
                        <div><span>Sá»‘ tráº» cáº§n chÄƒm</span><strong id="pv-kids"></strong></div>
                        <div><span>Tá»a Ä‘á»™ báº£n Ä‘á»“</span><strong id="pv-coords"></strong></div>
                        <div><span>Khoảng cách</span><strong id="pv-distance"></strong></div>
                    </div>
                </section>
                <section class="search-detail-card search-detail-card--wide">
                    <h3>Thông tin từ profile Phụ huynh</h3>
                    <div class="search-detail-info-grid">
                        <div class="search-detail-info-box"><p>Äáº·c Ä‘iá»ƒm</p><strong id="pv-characteristic"></strong></div>
                        <div class="search-detail-info-box"><p>NhÃ³m tuá»•i cá»§a bÃ©</p><strong id="pv-birthType"></strong></div>
                        <div class="search-detail-info-box"><p>Nhu cáº§u Ä‘áº·c biá»‡t</p><strong id="pv-specialNeeds"></strong></div>
                        <div class="search-detail-info-box"><p>Äá»™ tuá»•i báº£o máº«u</p><strong id="pv-ageRange"></strong></div>
                    </div>
                    <div class="search-detail-child-block">
                        <div class="search-detail-child-block__head">
                            <p class="search-detail-child-block__title">Danh sách từng bé</p>
                            <span class="search-detail-child-block__hint">Dá»¯ liá»‡u tá»« Hồ sơ trẻ</span>
                        </div>
                        <div id="pv-childProfiles" class="child-profile-stack child-profile-stack--preview hidden"></div>
                    </div>
                </section>
                <section class="search-detail-card search-detail-card--wide">
                    <h3>Ká»¹ nÄƒng yÃªu cáº§u</h3>
                    <div id="pv-skills" class="search-detail-skill-list"></div>
                </section>
                <section class="search-detail-card search-detail-card--wide">
                    <h3>Lá»‹ch ngÃ y muá»‘n tuyá»ƒn</h3>
                    <div id="pv-schedule"></div>
                </section>
                <section class="search-detail-card search-detail-card--wide">
                    <h3>Tráº¡ng thÃ¡i duyá»‡t</h3>
                    <div class="search-detail-moderation-box">
                        <strong id="pv-moderation"></strong>
                        <p id="pv-note" class="search-detail-note hidden"></p>
                    </div>
                </section>
            </div>
            <div id="pv-actionGrid" class="search-detail-bottom-action">
                <button id="pv-applyBtn"
                        type="button"
                        class="hidden search-detail-action-btn search-detail-action-btn--primary">
                    <span class="material-icons-round text-base">send</span>
                    <span id="pv-applyBtnText">á»¨ng tuyá»ƒn ngay</span>
                </button>
                <a id="pv-editBtn"
                   href="#"
                   class="hidden search-detail-action-btn search-detail-action-btn--secondary">
                    <span class="material-icons-round text-base">edit</span>
                    <span>Chá»‰nh sá»­a bÃ i Ä‘Äƒng</span>
                </a>
                <a id="pv-parentBtn" href="#" target="_blank" class="search-detail-action-btn search-detail-action-btn--ghost">
                    <span class="material-icons-round text-base">person</span>
                    <span id="pv-parentBtnText">Xem chi tiáº¿t há»“ sÆ¡ ngÆ°á»i Ä‘Äƒng</span>
                </a>
                <button id="pv-complainBtn"
                        type="button"
                        class="hidden py-3 rounded-xl border border-rose-200 text-rose-600 bg-rose-50 hover:bg-rose-100 font-bold text-sm transition-all inline-flex items-center justify-center gap-2 px-4">
                    <span class="material-icons-round text-base">flag</span>
                    <span>Pháº£n nÃ n bÃ i Ä‘Äƒng</span>
                </button>
            </div>
        </div>
    </div>
</div>

<div id="jobComplainModal" class="nmodal fixed inset-0 z-50 bg-black/50 backdrop-blur-sm items-center justify-center px-4" onclick="if(event.target===this)closeJobComplainModal()">
    <div class="nmodal-box bg-white rounded-2xl w-full max-w-xl shadow-2xl relative">
        <div class="flex items-center justify-between px-6 pt-6 pb-4 border-b border-gray-100">
            <div>
                <h2 class="text-lg font-bold text-gray-900">Gá»­i pháº£n nÃ n bÃ i Ä‘Äƒng</h2>
                <p id="jobComplainTarget" class="text-xs text-gray-400 font-semibold mt-0.5">BÃ i Ä‘Äƒng</p>
            </div>
            <button onclick="closeJobComplainModal()" class="text-gray-300 hover:text-gray-700 material-icons-round text-2xl transition-colors">close</button>
        </div>
        <form id="jobComplainForm" method="post" asp-controller="Complain" asp-action="ComplainJobPosting" class="px-6 py-5 space-y-4">
            @Html.AntiForgeryToken()
            <input type="hidden" id="jobComplainJobPostingId" name="JobPostingId" />
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Lý do phản nàn <span class="text-red-500">*</span></label>
                <textarea id="jobComplainReason" name="Reason" rows="4" class="w-full rounded-xl border border-gray-200 px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-orange-300 focus:border-orange-400" placeholder="Nhập lý do phản nàn..."></textarea>
            </div>
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Báº±ng chá»©ng (khÃ´ng báº¯t buá»™c)</label>
                <textarea id="jobComplainEvidence" name="Evidence" rows="3" class="w-full rounded-xl border border-gray-200 px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-orange-300 focus:border-orange-400" placeholder="Link, mÃ´ táº£ bá»• sung, thÃ´ng tin liÃªn quan..."></textarea>
            </div>
            <div class="flex gap-2">
                <button type="button" onclick="closeJobComplainModal()" class="modal-btn modal-btn-secondary w-full">Hủy</button>
                <button id="jobComplainSubmitBtn" type="submit" class="modal-btn modal-btn-primary w-full">Gửi phản nàn</button>
            </div>
        </form>
    </div>
</div>

<div id="historyModal" class="nmodal fixed inset-0 z-50 bg-black/50 backdrop-blur-sm items-center justify-center px-4" onclick="if(event.target===this)closeHistory()">
    <div class="nmodal-box bg-white rounded-2xl w-full max-w-3xl shadow-2xl relative">
        <div class="flex items-center justify-between px-6 pt-6 pb-4 border-b border-gray-100">
            <div>
                <h2 class="text-lg font-bold text-gray-900">Lá»‹ch sá»­ bÃ i Ä‘Äƒng</h2>
                <p class="text-xs text-gray-400 font-semibold mt-0.5">Xem tráº¡ng thÃ¡i chá» duyá»‡t, Ä‘Ã£ duyá»‡t, tá»« chá»‘i vÃ  ghi chÃº tá»« moderator.</p>
            </div>
            <button onclick="closeHistory()" class="text-gray-300 hover:text-gray-700 material-icons-round text-2xl transition-colors">close</button>
        </div>
        <div id="historyBody" class="px-6 py-5 max-h-[70vh] overflow-y-auto space-y-3">
            <div class="text-sm text-gray-500">Äang táº£i dá»¯ liá»‡u...</div>
        </div>
    </div>
</div>

@section Scripts {
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="~/lib/tinymce/tinymce.min.js" asp-append-version="true"></script>

@if (User.IsInRole("Nanny"))
{
<script>
// â”€â”€ Recommendation tab (Nanny) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
let recJobsLoaded = false;
let recUpgradeRequired = false;
window.recJobs = [];

function escRecHtml(v) {
    return String(v ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}

function recSalaryText(min, max, neg) {
    if (neg) return 'Thương lượng';
    if (min != null && max != null)
        return `${(min/1e6).toFixed(1).replace(/\.0$/,'')}M â€“ ${(max/1e6).toFixed(1).replace(/\.0$/,'')}M`;
    if (min != null) return `Từ ${(min/1e6).toFixed(1).replace(/\.0$/,'')}M`;
    return 'Thoáº£ thuáº­n';
}

function recScoreColor(pct) {
    if (pct >= 80) return '#16a34a';
    if (pct >= 60) return '#f97316';
    return '#6b7280';
}

function renderRecJobCard(j, idx) {
    const pct = Math.round((j.finalScore ?? 0) * 100);
    const color = recScoreColor(pct);
    const salary = recSalaryText(j.salaryMin, j.salaryMax, j.salaryNegotiable);
    const loc = j.city ? (j.district ? `${j.district}, ${j.city}` : j.city) : '';
    const distChip = j.distanceKm != null
        ? `<span class="rec-chip rec-chip--distance"><span class="material-icons-round" style="font-size:11px">near_me</span>${j.distanceKm.toFixed(1)} km</span>`
        : '';
    const salChip = salary
        ? `<span class="rec-chip rec-chip--salary"><span class="material-icons-round" style="font-size:11px">payments</span>${escRecHtml(salary)}</span>`
        : '';
    const skills = (j.requiredSkills || []).slice(0, 4)
        .map(s => `<span class="rec-chip rec-chip--skill">${escRecHtml(s.skillName ?? s)}</span>`).join('');
    const badgeCls = `rec-badge rec-badge--${j.matchTier || 'ok'}`;

    return `
    <div class="rec-card rec-card--clickable" data-rec-idx="${idx}" onclick="openRecPreview(${idx})">
        <div class="rec-card-header">
            <div class="rec-score-ring" style="--score-color:${color}">
                <span class="rec-score-value" style="color:${color}">${pct}%</span>
            </div>
            <div class="rec-card-meta">
                <h3 class="rec-card-title">${escRecHtml(j.title)}</h3>
                ${loc ? `<p class="rec-card-sub"><span class="material-icons-round" style="font-size:12px;vertical-align:middle">location_on</span> ${escRecHtml(loc)}</p>` : ''}
            </div>
        </div>
        <div class="rec-card-chips">
            ${salChip}${distChip}
        </div>
        ${skills ? `<div class="rec-card__skills" style="margin-top:8px">${skills}</div>` : ''}
        <div class="rec-card-footer">
            <span class="rec-ai-badge"><span class="material-icons-round" style="font-size:13px">auto_awesome</span>AI Gợi ý</span>
            <span class="${badgeCls}">${escRecHtml(j.matchLabel || '')}</span>
        </div>
    </div>`;
}

function openRecPreview(idx) {
    const j = window.recJobs[idx];
    if (!j) return;
    if (typeof openPreview !== 'function') return;
    openPreview({
        id:               j.jobId,
        title:            j.title,
        city:             j.city,
        district:         j.district,
        location:         j.district || j.city || '',
        salaryMin:        j.salaryMin,
        salaryMax:        j.salaryMax,
        salaryNegotiable: j.salaryNegotiable,
        distanceKm:       j.distanceKm,
        skills:           j.requiredSkills || [],
        scheduleSlots:    [],
        parentName:       null,
        jobType:          null,
        status:           null,
        numberOfChildren: null,
        characteristic:   null,
        birthTypeLabel:   null,
        specialNeeds:     null,
        minNannyAge:      null,
        maxNannyAge:      null,
        latitude:         j.latitude ?? null,
        longitude:        j.longitude ?? null,
        description:      j.description ?? null,
        moderationStatus: null,
        moderationNote:   null,
        parentProfileId:  null
    });
    // Fly to job marker on map
    if (typeof highlightOnMap === 'function') highlightOnMap(idx);
}

function plotRecJobMarkers(jobs) {
    if (typeof clearMapMarkers === 'function') clearMapMarkers();
    if (!window.map || !window.L) return;
    jobs.forEach((j, idx) => {
        if (!j.latitude || !j.longitude) return;
        const pct = Math.round((j.finalScore ?? 0) * 100);
        const color = recScoreColor(pct);
        const el = document.createElement('div');
        el.innerHTML = `<span style="font-size:11px;font-weight:800;color:${color}">${pct}%</span>`;
        el.style.cssText = `background:#fff;border:2px solid ${color};border-radius:999px;padding:2px 7px;white-space:nowrap;box-shadow:0 2px 8px rgba(0,0,0,.15);cursor:pointer`;
        const marker = window.L.marker([j.latitude, j.longitude], {
            icon: window.L.divIcon({ html: el, className: '', iconAnchor: [20, 12] })
        }).addTo(window.map);
        marker.bindPopup(`<div style="font-size:.82rem;font-weight:700">${escRecHtml(j.title)}</div><div style="font-size:.75rem;color:#64748b">${pct}% phù hợp</div>`);
        const circle = window.L.circle([j.latitude, j.longitude], {
            radius: 600, color: '#fdba74', fillColor: '#fdba74',
            fillOpacity: 0.1, weight: 1
        }).addTo(window.map);
        window.markers.push({ marker, circle, point: { lat: j.latitude, lng: j.longitude, zoom: 14 }, element: el });
    });
    // Fit map to rec markers
    const valid = jobs.filter(j => j.latitude && j.longitude);
    if (valid.length && window.map) {
        const bounds = window.L.latLngBounds(valid.map(j => [j.latitude, j.longitude]));
        window.suppressNextMapSearch = true;
        window.map.fitBounds(bounds, { padding: [32, 32], maxZoom: 14 });
    }
}

function bindRecJobHover() {
    document.querySelectorAll('.rec-card[data-rec-idx]').forEach(card => {
        const idx = Number(card.dataset.recIdx);
        card.addEventListener('mouseenter', () => {
            if (typeof setMarkerHover === 'function') setMarkerHover(idx, true, true);
        });
        card.addEventListener('mouseleave', () => {
            if (typeof setMarkerHover === 'function') setMarkerHover(idx, false, false);
        });
    });
}

function loadRecJobs() {
    if (recJobsLoaded || recUpgradeRequired) return;
    const content = document.getElementById('recJobContent');
    if (!content) return;

    fetch('/Recommendation/JobsData?topK=10')
        .then(r => r.json())
        .then(res => {
            if (res.requiresUpgrade) {
                recUpgradeRequired = true;
                if (typeof clearMapMarkers === 'function') clearMapMarkers();
                content.innerHTML = `
                <div class="rec-upgrade-prompt">
                    <span class="material-icons-round rec-upgrade-icon">auto_awesome</span>
                    <h3 class="rec-upgrade-title">TÃ­nh nÄƒng dÃ nh cho gÃ³i Plus / Pro</h3>
                    <p class="rec-upgrade-desc">NÃ¢ng cáº¥p Ä‘á»ƒ AI tá»± Ä‘á»™ng gá»£i Ã½ nhá»¯ng cÃ´ng viá»‡c phÃ¹ há»£p nháº¥t vá»›i há»“ sÆ¡ cá»§a báº¡n â€” tiáº¿t kiá»‡m thá»i gian tÃ¬m kiáº¿m.</p>
                    <a href="/Subscription" class="rec-upgrade-btn">
                        <span class="material-icons-round">rocket_launch</span>NÃ¢ng cáº¥p ngay
                    </a>
                </div>`;
                return;
            }
            if (!res.success || !res.data || res.data.length === 0) {
                if (typeof clearMapMarkers === 'function') clearMapMarkers();
                content.innerHTML = `<div class="rec-panel__empty">
                    <span class="material-icons-round">work_off</span>
                    <p>ChÆ°a cÃ³ viá»‡c phÃ¹ há»£p ngay lÃºc nÃ y.</p>
                    <p style="font-size:.78rem;margin-top:4px">HÃ£y cáº­p nháº­t ká»¹ nÄƒng vÃ  há»“ sÆ¡ Ä‘á»ƒ nháº­n gá»£i Ã½ tá»‘t hÆ¡n.</p>
                </div>`;
            } else {
                window.recJobs = res.data;
                content.innerHTML = `<div class="rec-card-list">${res.data.map((j, i) => renderRecJobCard(j, i)).join('')}</div>`;
                plotRecJobMarkers(res.data);
                bindRecJobHover();
            }
            recJobsLoaded = true;
        })
        .catch(() => {
            content.innerHTML = `<div class="rec-panel__empty">
                <span class="material-icons-round">wifi_off</span><p>KhÃ´ng táº£i Ä‘Æ°á»£c gá»£i Ã½.</p>
            </div>`;
        });
}

function switchSearchTab(tab) {
    const tabAll      = document.getElementById('tabAll');
    const tabRec      = document.getElementById('tabRec');
    const jobList     = document.getElementById('jobList');
    const recPanel    = document.getElementById('recJobPanel');
    const resultCount = document.getElementById('resultCount');
    const searchBar   = document.getElementById('searchCity')?.parentElement;

    if (tab === 'rec') {
        tabAll.classList.remove('active');
        tabRec.classList.add('active');
        jobList.style.display = 'none';
        if (resultCount) resultCount.style.display = 'none';
        if (searchBar)   searchBar.style.display = 'none';
        recPanel.classList.add('active');
        // Re-plot náº¿u Ä‘Ã£ cÃ³ data, load láº§n Ä‘áº§u náº¿u chÆ°a
        if (recJobsLoaded && window.recJobs.length) {
            plotRecJobMarkers(window.recJobs);
            bindRecJobHover();
        } else {
            loadRecJobs();
        }
    } else {
        tabRec.classList.remove('active');
        tabAll.classList.add('active');
        jobList.style.display = '';
        if (resultCount) resultCount.style.display = '';
        if (searchBar)   searchBar.style.display = '';
        recPanel.classList.remove('active');
        // XoÃ¡ rec markers, hiá»‡n láº¡i job markers thÆ°á»ng
        if (typeof clearMapMarkers === 'function') clearMapMarkers();
        if (typeof fetchJobs === 'function') fetchJobs();
    }
}
</script>
}

<script>
const API_URL = '/Search/Jobs';
const IS_AUTH = @(isAuthenticated ? "true" : "false");
const IS_PARENT = @(isParent ? "true" : "false");
const IS_NANNY = @(isNanny ? "true" : "false");
const CURRENT_USER_NAME = '@userName';

</script>
<script src="~/js/search.js" asp-append-version="true"></script>
}


