<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>@ViewData["Title"] - Moderator | NannyMatch</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet" />
    <link href="~/css/moderator.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.7/signalr.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { display: ['Quicksand', 'sans-serif'] },
                    colors: {
                        primary: '#F97316',
                        primaryDark: '#EA580C',
                    },
                    boxShadow: {
                        'card': '0 4px 24px rgba(0,0,0,0.07)',
                        'soft': '0 8px 32px rgba(0,0,0,0.08)',
                    }
                }
            }
        }
    </script>
    <style>
        .nm-icon-btn {
            position: relative;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 44px;
            height: 44px;
            border-radius: 16px;
            border: 1px solid rgba(251, 146, 60, .16);
            background: rgba(255,255,255,.88);
            color: #6b7280;
            transition: all .2s ease;
            box-shadow: 0 10px 24px rgba(15, 23, 42, .06);
        }

        .nm-icon-btn:hover {
            color: #ea580c;
            background: #fff7ed;
            transform: translateY(-1px);
        }

        .nm-badge-dot {
            position: absolute;
            top: 6px;
            right: 6px;
            min-width: 18px;
            height: 18px;
            padding: 0 4px;
            border-radius: 999px;
            background: linear-gradient(135deg, #f97316, #ea580c);
            color: #fff;
            font-size: 11px;
            font-weight: 800;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 16px rgba(249,115,22,.26);
        }

        .nm-panel {
            position: absolute;
            right: 0;
            top: calc(100% + 12px);
            width: min(380px, calc(100vw - 24px));
            background: rgba(255,255,255,.98);
            border: 1px solid rgba(226,232,240,.92);
            border-radius: 24px;
            box-shadow: 0 26px 60px rgba(15, 23, 42, .14);
            overflow: hidden;
        }

        .nm-panel-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            padding: 16px 18px 12px;
            border-bottom: 1px solid rgba(241,245,249,.95);
        }

        .nm-panel-title {
            font-size: 15px;
            font-weight: 800;
            color: #111827;
        }

        .nm-panel-link {
            color: #ea580c;
            font-size: 13px;
            font-weight: 700;
        }

        .nm-notification-list {
            max-height: 360px;
            overflow: auto;
            padding: 8px;
        }

        .nm-notification-item {
            display: block;
            width: 100%;
            text-align: left;
            padding: 14px;
            border-radius: 18px;
            transition: background .18s ease, transform .18s ease;
        }

        .nm-notification-item:hover {
            background: #fff7ed;
            transform: translateY(-1px);
        }

        .nm-notification-item.unread {
            background: linear-gradient(135deg, rgba(255,247,237,.92), rgba(255,255,255,.96));
        }

        .nm-notification-item-title {
            font-size: 14px;
            font-weight: 800;
            color: #111827;
            margin-bottom: 4px;
        }

        .nm-notification-item-body {
            font-size: 13px;
            line-height: 1.5;
            color: #6b7280;
            margin-bottom: 8px;
        }

        .nm-notification-item-meta {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
            margin-bottom: 8px;
        }

        .nm-notification-chip {
            display: inline-flex;
            align-items: center;
            min-height: 22px;
            padding: 0 8px;
            border-radius: 999px;
            background: #fff7ed;
            color: #c2410c;
            font-size: 11px;
            font-weight: 800;
        }

        .nm-notification-sender {
            font-size: 12px;
            color: #9ca3af;
            font-weight: 700;
        }

        .nm-notification-item-time {
            font-size: 12px;
            color: #9ca3af;
        }

        .nm-empty-state {
            padding: 28px 18px 30px;
            text-align: center;
            color: #6b7280;
        }

        .nm-empty-state .material-icons-round {
            font-size: 28px;
            color: #fdba74;
            margin-bottom: 8px;
        }

        .nm-toast-root {
            position: fixed;
            right: 18px;
            bottom: 18px;
            z-index: 12050;
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            gap: 10px;
            pointer-events: none;
        }

        .nm-toast {
            width: min(420px, calc(100vw - 24px));
            display: grid;
            grid-template-columns: auto 1fr auto;
            align-items: center;
            gap: 9px;
            padding: 12px 14px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, .14);
            box-shadow: 0 18px 36px rgba(15, 23, 42, .2);
            color: #fff;
            transform: translateY(12px) scale(.98);
            opacity: 0;
            transition: opacity .22s ease, transform .22s ease;
            pointer-events: auto;
            backdrop-filter: blur(6px);
        }

        .nm-toast.show {
            opacity: 1;
            transform: translateY(0) scale(1);
        }

        .nm-toast__icon {
            width: 22px;
            height: 22px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            border-radius: 999px;
            background: rgba(255,255,255,.78);
            color: rgba(15,23,42,.68);
        }

        .nm-toast__text {
            font-size: 13px;
            line-height: 1.45;
            font-weight: 800;
            word-break: break-word;
        }

        .nm-toast__close {
            width: 22px;
            height: 22px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 999px;
            border: none;
            background: rgba(255, 255, 255, .22);
            color: #fff;
            cursor: pointer;
            font-size: 17px;
            transition: background .18s ease, transform .18s ease;
        }

        .nm-toast__close:hover {
            background: rgba(255, 255, 255, .35);
            transform: scale(1.03);
        }

        .nm-toast--info {
            background: linear-gradient(135deg, rgba(15,23,42,.94), rgba(30,41,59,.94));
        }

        .nm-toast--success {
            background: linear-gradient(135deg, #0f8c67, #10946d);
        }

        .nm-toast--warning {
            background: linear-gradient(135deg, #c56a09, #ea580c);
        }

        .nm-toast--error {
            background: linear-gradient(135deg, #c22525, #dc2626);
        }
    </style>
</head>
<body class="font-display bg-gray-50 text-gray-800" style="font-family:'Quicksand',sans-serif;">

    <div class="flex h-screen overflow-hidden">

        <!-- ===== SIDEBAR ===== -->
        <aside id="sidebar" class="mod-sidebar fixed inset-y-0 left-0 z-40 flex flex-col bg-gradient-to-b from-gray-900 via-gray-900 to-gray-950 text-white transition-all duration-300 overflow-hidden"
               style="width: 260px;">

            <!-- Brand -->
            <div class="flex items-center gap-3 px-5 py-5 border-b border-white/10">
                <img src="~/img/nanny-logo.jpg" alt="Logo" class="w-9 h-9 rounded-xl object-cover shadow-lg flex-shrink-0" />
                <div class="sidebar-text overflow-hidden">
                    <span class="text-lg font-bold tracking-tight" style="background:linear-gradient(135deg,#F97316,#FB923C);-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;">NannyMatch</span>
                    <p class="text-[10px] text-gray-400 -mt-0.5 uppercase tracking-widest">Moderator Panel</p>
                </div>
            </div>

            <!-- Nav -->
            <nav class="flex-1 px-3 py-5 space-y-1 overflow-y-auto overflow-x-hidden">

                <p class="sidebar-text px-2 text-[10px] uppercase tracking-widest text-gray-500 mb-2 font-semibold">Overview</p>

                <a href="/Moderator/Dashboard"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() == "Dashboard" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">dashboard</span>
                    <span class="sidebar-text">Dashboard</span>
                </a>

                <p class="sidebar-text px-2 text-[10px] uppercase tracking-widest text-gray-500 mt-5 mb-2 font-semibold">Management</p>

                <a href="/Moderator/ManageAccount"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() == "ManageAccount" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">manage_accounts</span>
                    <span class="sidebar-text">Manage Accounts</span>
                </a>

                <a href="/Moderator/ManageNannyVerification"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() == "NannyVerification" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">verified_user</span>
                    <span class="sidebar-text">Manage Nanny Verification Requests</span>
                </a>

                 

                <a href="/Moderator/ManageJobPosting"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() == "ManageJobPosting" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">work</span>
                    <span class="sidebar-text">Manage Job Postings</span>
                </a>
                <a href="/Moderator/ManageReport"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() == "ViewReports" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">flag</span>
                    <span class="sidebar-text">Reports</span>
                </a>

                <p class="sidebar-text px-2 text-[10px] uppercase tracking-widest text-gray-500 mt-5 mb-2 font-semibold">Content</p>

                <a href="/Moderator/ManageBlog"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() is "ManageBlog" or "CreateBlog" or "ViewBlogDetail" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">article</span>
                    <span class="sidebar-text">Manage Blogs</span>
                </a>

                <a href="/Moderator/ManageFAQ"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() is "ManageFAQ" or "CreateFAQ" or "ViewFAQDetail" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">help_outline</span>
                    <span class="sidebar-text">Manage FAQ</span>
                </a>

                <a href="/Moderator/ManageBlogCategory"
                   class="nav-link flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-200 @(ViewContext.RouteData.Values["action"]?.ToString() is "ManageBlogCategory" or "CreateBlogCategory" or "ViewBlogCategoryDetail" ? "bg-orange-500/20 text-orange-400 border border-orange-500/30" : "text-gray-300 hover:bg-white/8 hover:text-white")">
                    <span class="material-icons-round text-xl flex-shrink-0">category</span>
                    <span class="sidebar-text">Manage Blog Categories</span>
                </a>
            </nav>

            <!-- Bottom: User info + logout -->
            <div class="p-4 border-t border-white/10">
                <div class="flex items-center gap-3">
                    <div class="w-9 h-9 rounded-full bg-gradient-to-br from-orange-400 to-orange-600 flex items-center justify-center text-white font-bold text-sm flex-shrink-0">
                        @(User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value?.Substring(0, 1).ToUpper() ?? "M")
                    </div>
                    <div class="sidebar-text flex-1 min-w-0">
                        <p class="text-sm font-semibold text-white truncate">
                            @(User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? "Moderator")
                        </p>
                        <p class="text-xs text-gray-400 truncate">Moderator</p>
                    </div>
                    <form asp-controller="Auth" asp-action="Logout" method="post" class="sidebar-text flex-shrink-0">
                        @Html.AntiForgeryToken()
                        <button type="submit" title="Sign out"
                                class="w-8 h-8 rounded-lg hover:bg-red-500/20 flex items-center justify-center text-gray-400 hover:text-red-400 transition-all">
                            <span class="material-icons-round text-xl">logout</span>
                        </button>
                    </form>
                </div>
            </div>
        </aside>

        <!-- ===== MAIN CONTENT ===== -->
        <div id="mainContent" class="flex flex-col flex-1 min-w-0 transition-all duration-300" style="margin-left: 260px;">

            <!-- Top Header Bar -->
            <header class="sticky top-0 z-30 bg-white/95 backdrop-blur border-b border-gray-100 flex flex-col"
                    style="box-shadow:0 2px 20px rgba(249,115,22,0.07);">
                <!-- Shimmer accent line -->
                <div class="mod-topbar-accent"></div>
                <!-- Header row -->
                <div class="flex items-center justify-between px-6 h-15" style="height:60px;">
                    <div class="flex items-center gap-4">
                        <button id="sidebarToggle" onclick="toggleSidebar()"
                                class="w-9 h-9 rounded-xl hover:bg-orange-50 flex items-center justify-center text-gray-400 hover:text-orange-500 transition-all">
                            <span class="material-icons-round">menu</span>
                        </button>
                        <div class="flex items-center gap-2 text-sm">
                            <span class="text-gray-400">NannyMatch</span>
                            <span class="material-icons-round text-gray-300 text-sm">chevron_right</span>
                            <span class="font-bold" style="background:linear-gradient(135deg,#F97316,#EA580C);-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;">@ViewData["Title"]</span>
                        </div>
                    </div>

                    <div class="flex items-center gap-3">
                        <div class="relative" id="notificationDropdown">
                            <button type="button"
                                    id="notificationToggle"
                                    class="nm-icon-btn"
                                    aria-label="Thong bao">
                                <span class="material-icons-round text-[20px]">notifications</span>
                                <span id="notificationBadge" class="nm-badge-dot hidden">0</span>
                            </button>

                            <div id="notificationMenu" class="nm-panel hidden">
                                <div class="nm-panel-header">
                                    <div>
                                        <p class="nm-panel-title">Thong bao</p>
                                        <p class="text-xs text-gray-500">Cong viec moi va cac cap nhat can moderator xu ly</p>
                                    </div>
                                    <button type="button" id="markAllNotificationsRead" class="nm-panel-link">
                                        Danh dau da doc
                                    </button>
                                </div>
                                <div id="notificationList" class="nm-notification-list">
                                    <div class="nm-empty-state">
                                        <span class="material-icons-round">notifications_none</span>
                                        <p class="text-sm font-semibold text-gray-700">Dang tai thong bao...</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="flex items-center gap-2 pl-3 border-l border-gray-100">
                            <div class="w-8 h-8 rounded-full bg-gradient-to-br from-orange-400 to-orange-600 flex items-center justify-center text-white font-bold text-sm shadow-md">
                                @(User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value?.Substring(0, 1).ToUpper() ?? "M")
                            </div>
                            <span class="text-sm font-semibold text-gray-700 hidden sm:block">
                                @(User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? "Moderator")
                            </span>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Page Content -->
            <main class="flex-1 overflow-y-auto p-6">
                @RenderBody()
            </main>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        var sidebarCollapsed = false;

        function toggleSidebar() {
            var sidebar = document.getElementById('sidebar');
            var main = document.getElementById('mainContent');
            sidebarCollapsed = !sidebarCollapsed;

            if (sidebarCollapsed) {
                sidebar.style.width = '72px';
                main.style.marginLeft = '72px';
                sidebar.classList.add('collapsed');
            } else {
                sidebar.style.width = '260px';
                main.style.marginLeft = '260px';
                sidebar.classList.remove('collapsed');
            }
        }

        (() => {
            const notificationDropdown = document.getElementById('notificationDropdown');
            const notificationToggle = document.getElementById('notificationToggle');
            const notificationMenu = document.getElementById('notificationMenu');
            const notificationBadge = document.getElementById('notificationBadge');
            const notificationList = document.getElementById('notificationList');
            const markAllButton = document.getElementById('markAllNotificationsRead');

            if (!notificationDropdown || !notificationToggle || !notificationMenu || !notificationBadge || !notificationList || !markAllButton) {
                return;
            }

            const escapeHtml = (value) => String(value ?? '')
                .replaceAll('&', '&amp;')
                .replaceAll('<', '&lt;')
                .replaceAll('>', '&gt;')
                .replaceAll('"', '&quot;')
                .replaceAll("'", '&#39;');

            const formatTime = (value) => {
                if (!value) return '';
                const date = new Date(value);
                if (Number.isNaN(date.getTime())) return '';
                return new Intl.DateTimeFormat('vi-VN', {
                    day: '2-digit',
                    month: '2-digit',
                    year: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }).format(date);
            };

            const renderEmpty = (message) => {
                notificationList.innerHTML = `
                    <div class="nm-empty-state">
                        <span class="material-icons-round">notifications_none</span>
                        <p class="text-sm font-semibold text-gray-700">${escapeHtml(message)}</p>
                    </div>`;
            };

            const updateBadge = (count) => {
                if (!count || count <= 0) {
                    notificationBadge.classList.add('hidden');
                    return;
                }

                notificationBadge.textContent = count > 99 ? '99+' : String(count);
                notificationBadge.classList.remove('hidden');
            };

            const loadUnreadCount = async () => {
                try {
                    const response = await fetch('/Notification/UnreadCount', { credentials: 'same-origin' });
                    const result = await response.json();
                    updateBadge(result?.data?.unreadCount ?? 0);
                } catch {
                    updateBadge(0);
                }
            };

            const loadNotifications = async () => {
                try {
                    const response = await fetch('/Notification/My?page=1&pageSize=8', { credentials: 'same-origin' });
                    const result = await response.json();
                    const items = result?.data ?? [];
                    const unreadCount = result?.unreadCount ?? 0;
                    updateBadge(unreadCount);

                    if (!items.length) {
                        renderEmpty('Moderator chua co thong bao nao.');
                        return;
                    }

                    notificationList.innerHTML = items.map(item => `
                        <button type="button"
                                class="nm-notification-item ${item.isRead ? '' : 'unread'}"
                                data-id="${escapeHtml(item.id)}"
                                data-url="${escapeHtml(item.actionUrl || '')}">
                            <div class="nm-notification-item-title">${escapeHtml(item.title)}</div>
                            <div class="nm-notification-item-meta">
                                <span class="nm-notification-chip">${escapeHtml(item.typeLabel || 'Thong bao')}</span>
                                <span class="nm-notification-sender">Tu ${escapeHtml(item.senderLabel || 'He thong')}</span>
                            </div>
                            <div class="nm-notification-item-body">${escapeHtml(item.content)}</div>
                            <div class="nm-notification-item-time">${escapeHtml(formatTime(item.createdAt))}</div>
                        </button>
                    `).join('');
                } catch {
                    renderEmpty('Khong the tai thong bao luc nay.');
                }
            };

            const getRealtimeToastConfig = (payload) => {
                if (!payload) return null;

                const type = String(payload.type || '').trim().toLowerCase();
                const message = payload.message || payload.title;
                if (message) {
                    return {
                        message,
                        type: payload.toastType || 'info'
                    };
                }

                switch (type) {
                    case 'verification-request-submitted':
                        return { message: 'Co yeu cau xac minh moi dang cho duyet.', type: 'info' };
                    case 'job-posting-review-required':
                        return { message: 'Co bai dang moi dang cho moderator duyet.', type: 'info' };
                    case 'report-submitted':
                        return { message: 'Co bao cao moi can xu ly.', type: 'warning' };
                    case 'message-to-moderator':
                        return { message: 'Co tin nhan moi gui toi moderator.', type: 'info' };
                    case 'system-warning':
                        return { message: 'Co canh bao he thong can xu ly.', type: 'warning' };
                    default:
                        return null;
                }
            };

            notificationToggle.addEventListener('click', async () => {
                notificationMenu.classList.toggle('hidden');
                if (!notificationMenu.classList.contains('hidden')) {
                    await loadNotifications();
                }
            });

            notificationList.addEventListener('click', async (event) => {
                const item = event.target.closest('.nm-notification-item[data-id]');
                if (!item) return;

                const actionUrl = item.dataset.url;

                try {
                    await fetch(`/Notification/MarkRead/${item.dataset.id}`, {
                        method: 'POST',
                        credentials: 'same-origin'
                    });
                } catch {
                }

                item.classList.remove('unread');
                await loadUnreadCount();

                if (actionUrl) {
                    window.location.href = actionUrl;
                }
            });

            markAllButton.addEventListener('click', async () => {
                try {
                    await fetch('/Notification/MarkAllRead', {
                        method: 'POST',
                        credentials: 'same-origin'
                    });
                } catch {
                }

                await loadNotifications();
            });

            window.addEventListener('nm:notifications-refresh', async () => {
                await loadUnreadCount();
                if (!notificationMenu.classList.contains('hidden')) {
                    await loadNotifications();
                }
            });

            document.addEventListener('click', function(e) {
                if (!notificationDropdown.contains(e.target)) {
                    notificationMenu.classList.add('hidden');
                }
            });

            const connectNotificationRealtime = async () => {
                if (!window.signalR) return;

                const connection = new signalR.HubConnectionBuilder()
                    .withUrl('/hubs/notifications')
                    .withAutomaticReconnect()
                    .build();

                connection.on('notification:new', async (payload) => {
                    const toastConfig = getRealtimeToastConfig(payload);
                    if (toastConfig && typeof window.showToast === 'function') {
                        window.showToast(toastConfig.message, { type: toastConfig.type });
                    }
                    window.dispatchEvent(new CustomEvent('nm:notifications-refresh'));
                });

                connection.onreconnected(() => {
                    window.dispatchEvent(new CustomEvent('nm:notifications-refresh'));
                });

                try {
                    await connection.start();
                } catch {
                }
            };

            loadUnreadCount();
            connectNotificationRealtime();
        })();
    </script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        (() => {
            const params = new URLSearchParams(window.location.search);
            const toastMessage = params.get('toastMessage');
            const toastType = params.get('toastType') || 'info';

            if (!toastMessage || typeof window.showToast !== 'function') {
                return;
            }

            window.showToast(toastMessage, { type: toastType });
            params.delete('toastMessage');
            params.delete('toastType');

            const nextQuery = params.toString();
            const nextUrl = `${window.location.pathname}${nextQuery ? `?${nextQuery}` : ''}${window.location.hash || ''}`;
            window.history.replaceState({}, document.title, nextUrl);
        })();
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
